document.addEventListener('DOMContentLoaded',()=>BRM.initPrivatePage(async()=>{
  const main=document.querySelector('#app-main');
  main.innerHTML=`<div class="page-head"><div><span class="eyebrow">Installed-app readiness</span><h1>Offline & storage</h1><p>Control downloaded rehearsal music, temporary images, saved portal data, and offline reliability on this device.</p></div><div class="page-actions"><button class="button button-secondary" data-refresh>Refresh report</button></div></div><div class="alert alert-info" data-connectivity><strong>Checking connection…</strong></div><section class="command-summary" data-storage-summary></section><section class="grid grid-2 storage-control-grid"><article class="panel"><span class="eyebrow">Recommended</span><h2>Protect offline data</h2><p>Ask this device not to automatically remove downloaded music and saved production data when storage becomes tight.</p><div data-persistence-status class="storage-status"></div><button class="button button-primary" data-persist>Protect offline storage</button></article><article class="panel"><span class="eyebrow">Rehearsal audio</span><h2>Downloaded tracks</h2><p data-audio-detail>Inspecting the offline music library…</p><div class="card-actions"><a class="button button-primary" href="tracks.html">Manage music downloads</a><button class="button button-danger" data-clear-audio>Remove all downloaded music</button></div></article><article class="panel"><span class="eyebrow">Temporary media</span><h2>Image and runtime cache</h2><p>Images are kept after first use for quicker loading. Clearing them does not remove uploads from Drive.</p><p class="field-hint" data-runtime-detail></p><button class="button button-secondary" data-clear-runtime>Clear temporary images</button></article><article class="panel"><span class="eyebrow">Application files</span><h2>Offline portal shell</h2><p>HTML, CSS and JavaScript let the portal open when the network is unavailable. Refresh this after troubleshooting an update.</p><p class="field-hint" data-shell-detail></p><button class="button button-secondary" data-refresh-shell>Refresh application files</button></article><article class="panel"><span class="eyebrow">Personal data</span><h2>Saved portal snapshot</h2><p>The last successful portal response makes schedules, tasks and announcements available during a connection problem.</p><button class="button button-secondary" data-clear-snapshot>Clear saved portal data</button></article><article class="panel"><span class="eyebrow">Privacy</span><h2>Shared device?</h2><p>Signing out removes the session, user-specific portal snapshot, and locally cached profile photos. Downloaded music remains available to the production app.</p><button class="button button-danger" data-sign-out>Clear account data and sign out</button></article></section>`;
  const formatBytes=bytes=>{const value=Number(bytes||0);if(value<1024)return`${value} B`;if(value<1048576)return`${(value/1024).toFixed(1)} KB`;if(value<1073741824)return`${(value/1048576).toFixed(1)} MB`;return`${(value/1073741824).toFixed(2)} GB`};
  const cacheSize=async predicate=>{if(!('caches'in window))return 0;let total=0;for(const name of await caches.keys()){if(!predicate(name))continue;const cache=await caches.open(name);for(const request of await cache.keys()){const response=await cache.match(request);try{total+=(await response.clone().blob()).size}catch{}}}return total};
  const audioEntries=()=>new Promise(resolve=>{if(!('indexedDB'in window)){resolve([]);return}const request=indexedDB.open('bedford-musical-audio-v1',1);request.onupgradeneeded=()=>{if(!request.result.objectStoreNames.contains('tracks'))request.result.createObjectStore('tracks',{keyPath:'trackId'})};request.onerror=()=>resolve([]);request.onsuccess=()=>{const db=request.result,tx=db.transaction('tracks','readonly'),get=tx.objectStore('tracks').getAll();get.onsuccess=()=>resolve(get.result||[]);get.onerror=()=>resolve([]);tx.oncomplete=()=>db.close()}});
  const clearAudio=()=>new Promise((resolve,reject)=>{const request=indexedDB.open('bedford-musical-audio-v1',1);request.onerror=()=>reject(request.error);request.onsuccess=()=>{const db=request.result,tx=db.transaction('tracks','readwrite');tx.objectStore('tracks').clear();tx.oncomplete=()=>{db.close();resolve()};tx.onerror=()=>{db.close();reject(tx.error)}}});
  async function report(){const [estimate,persisted,audio,shellBytes,runtimeBytes]=await Promise.all([navigator.storage?.estimate?.().catch(()=>null),navigator.storage?.persisted?.().catch(()=>false),audioEntries(),cacheSize(name=>name.startsWith('bedford-shell-')),cacheSize(name=>name==='bedford-runtime-v1'||name.startsWith('bedford-profile-photos-'))]),audioBytes=audio.reduce((sum,item)=>sum+Number(item.byteLength||item.blob?.size||0),0);main.querySelector('[data-storage-summary]').innerHTML=`<div class="command-stat"><strong>${formatBytes(estimate?.usage||0)}</strong><span>Total site storage</span></div><div class="command-stat"><strong>${estimate?.quota?formatBytes(estimate.quota):'Unknown'}</strong><span>Storage allowance</span></div><div class="command-stat"><strong>${audio.length}</strong><span>Downloaded tracks</span></div><div class="command-stat ${persisted?'':'attention'}"><strong>${persisted?'Protected':'Temporary'}</strong><span>Offline retention</span></div>`;main.querySelector('[data-persistence-status]').innerHTML=persisted?'<div class="alert alert-success"><strong>Protected:</strong> this browser granted persistent storage.</div>':'<div class="alert alert-info"><strong>Not protected yet:</strong> Android may remove offline data if device storage becomes low.</div>';main.querySelector('[data-persist]').hidden=Boolean(persisted);main.querySelector('[data-audio-detail]').textContent=`${audio.length} track${audio.length===1?'':'s'} · ${formatBytes(audioBytes)} stored locally`;main.querySelector('[data-runtime-detail]').textContent=`Approximately ${formatBytes(runtimeBytes)} currently cached.`;main.querySelector('[data-shell-detail]').textContent=`Approximately ${formatBytes(shellBytes)} of application files cached.`}
  const connectivity=()=>{const online=navigator.onLine;main.querySelector('[data-connectivity]').className=`alert ${online?'alert-success':'alert-error'}`;main.querySelector('[data-connectivity]').innerHTML=`<strong>${online?'Online':'Offline'}:</strong> ${online?'live production data can refresh in the background.':'the portal is using information already saved on this device.'}`};
  main.querySelector('[data-refresh]').onclick=report;main.querySelector('[data-persist]').onclick=async()=>{const granted=await navigator.storage?.persist?.();BRM.toast(granted?'Offline storage is now protected.':'Android did not grant protected storage yet.',granted?'info':'error');report()};main.querySelector('[data-clear-audio]').onclick=async()=>{if(!confirm('Remove every downloaded rehearsal track from this device?'))return;await clearAudio();BRM.toast('Downloaded music removed.');report()};main.querySelector('[data-clear-runtime]').onclick=async()=>{if(!confirm('Clear temporary images and runtime files? They will download again when needed.'))return;for(const name of await caches.keys())if(name==='bedford-runtime-v1'||name.startsWith('bedford-profile-photos-'))await caches.delete(name);BRM.toast('Temporary media cleared.');report()};main.querySelector('[data-refresh-shell]').onclick=async()=>{navigator.serviceWorker?.controller?.postMessage('CLEAR_SHELL_CACHE');BRM.toast('Refreshing application files…','info');setTimeout(()=>location.reload(),500)};main.querySelector('[data-clear-snapshot]').onclick=async()=>{await BRM.clearSiteCache?.();BRM.toast('Saved portal snapshot cleared.');report()};main.querySelector('[data-sign-out]').onclick=()=>{if(!confirm('Clear account data and sign out on this device?'))return;BRM.clearSession();location.href='login.html'};window.addEventListener('online',connectivity);window.addEventListener('offline',connectivity);connectivity();await report();
}));
